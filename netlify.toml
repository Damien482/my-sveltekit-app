[build]
  command = "npm run build"
  publish = "build"

[functions]
  directory = "netlify/functions"

# Optional but commonly useful: cache immutable assets longer
[[headers]]
  for = "/_app/immutable/*"
  [headers.values]
    cache-control = "public, max-age=31536000, immutable"
    